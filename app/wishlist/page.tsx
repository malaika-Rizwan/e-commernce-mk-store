import { WishlistContent } from './WishlistContent';

export const metadata = {
  title: 'Wishlist',
  description: 'Your saved products. Sign in to sync your wishlist across devices.',
};

export default function WishlistPage() {
  return <WishlistContent />;
}
